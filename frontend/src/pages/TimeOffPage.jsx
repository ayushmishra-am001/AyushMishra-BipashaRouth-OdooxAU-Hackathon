import { PagePlaceholder } from './PagePlaceholder';

export default function TimeOffPage() {
  return (
    <PagePlaceholder
      title="Time Off"
      description="Leave balances, the apply-leave modal, and approvals are built in Step 8."
    />
  );
}
